# 调试指南（V3）

## 1. 前端调试

1. 启动开发环境：

```bash
npm run tauri:dev
```

2. 打开 DevTools（F12）查看：
- Console 报错
- Network 请求
- 组件状态

## 2. 常见前端问题

### 2.1 页面空白

- 检查控制台是否有运行时异常
- 检查路由是否正确加载

### 2.2 快照测试失败

- 先确认是否为预期 UI 变更
- 如为预期，更新快照：

```bash
npm run test -- VisualSnapshot -u
```

### 2.3 中文显示乱码

- 执行编码修复：

```bash
npm run specs:fix-encoding
npm run specs:check-encoding
```

## 3. Rust 调试

在 `src-tauri` 目录执行：

```bash
cargo test -- --nocapture
```

针对关键模块：

```bash
cargo test plot_consistency -- --nocapture
cargo test plot_engine -- --nocapture
```

## 4. 提交前排查建议

1. 先最小复现问题
2. 再增加日志确认根因
3. 最后补测试防回归
