# Web 核心流程冒烟

目标：快速确认 Web 适配层能跑通最小可玩主链路。

## 覆盖范围
- 生成/加载剧本
- 初始化游戏与剧情
- 执行一次玩家行动
- 存档与读档

## 执行命令
```bash
npm run test:web-core
```

## 判定标准
- 测试通过：说明 Web mock 运行时的核心主流程可用。
- 测试失败：优先检查 `src/platform/webRuntime.ts` 的命令实现是否与前端调用参数一致。
