# UI 手工测试记录（V3）

- 日期：2026-02-20
- 测试范围：主菜单、剧本选择、游戏主界面、信息抽屉、系统中心、存档流程
- 测试环境：Windows（Tauri 本地开发环境）
- 前端版本：V3 UI 重构后版本

## 测试用例与结果

1. 新游戏流程
- 步骤：进入主菜单 -> 点击新游戏 -> 选择剧本 -> 开始
- 预期：顺利进入 `GameRuntimeView`
- 结果：通过

2. 剧情推进（选项模式）
- 步骤：选择任一可用选项并提交
- 预期：剧情追加，状态区刷新
- 结果：通过

3. 剧情推进（自由输入）
- 步骤：切换自由输入 -> 输入文本 -> 提交
- 预期：输入被处理并返回剧情反馈
- 结果：通过

4. 自动推进中断
- 步骤：触发继续推进 -> 在加载态点击中断
- 预期：自动推进停止，界面恢复可交互
- 结果：通过

5. 信息抽屉
- 步骤：打开右侧信息抽屉 -> 切换地图/复盘/导出页签
- 预期：页签切换正常，内容按需展示
- 结果：通过

6. 系统中心入口
- 步骤：打开系统中心菜单 -> 进入 LLM 设置/剧情策略/一致性策略
- 预期：入口统一且可达
- 结果：通过

7. 存档与读档
- 步骤：打开保存对话框保存 -> 打开读档对话框加载
- 预期：槽位可见，加载后恢复状态
- 结果：通过

8. 键盘可达性
- 步骤：使用 `Tab` 逐项聚焦，`Esc` 关闭弹层
- 预期：焦点环可见，顺序合理，`Esc` 生效
- 结果：通过

9. 响应式布局
- 步骤：检查 1280+、1024、768、移动窄屏下布局
- 预期：核心阅读与交互区域不重叠，抽屉宽度适配
- 结果：通过

10. 通知中心
- 步骤：触发状态提示与可关闭通知
- 预期：Banner/Toast 显示正常，可关闭
- 结果：通过

## 缺陷清单

- 本轮未发现 P0/P1 缺陷。
- 观察项：
  - 极窄屏设备下，抽屉与输入区切换频繁时存在轻微视觉跳动（可后续优化过渡曲线）。

## 增量回归（本轮）

11. 位置文案中文化回归
- 步骤：打开状态卡与信息抽屉地图页，检查当前位置与节点名称显示
- 预期：不出现 `sect_valley` 等技术 key，显示用户可读文案
- 结果：通过

12. 章节阅读顶部留白回归
- 步骤：进入章节阅读区，观察章节标题/摘要卡/正文之间垂直间距
- 预期：正文进入前无大块无意义空白
- 结果：通过

13. 中等宽度状态卡密度回归
- 步骤：在 `sm` 断点观察状态卡布局
- 预期：状态卡优先三列展示，不再纵向堆叠挤压阅读区
- 结果：通过
