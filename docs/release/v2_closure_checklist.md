# V2 结项确认清单（A-E）

更新日期：2026-02-19

## A. 字段定义无歧义
- 结论：已确认。
- 依据：V2 核心链路已落地并在 Rust/前端类型中形成稳定字段。
- 覆盖范围：
  - LLM 输出契约与协作协议：任务 17/18。
  - 实体结构化与数值守门：任务 19A/19B。
  - 分层记忆与上下文协同：任务 19C。
  - 导出真实经历与章节系统：任务 22B/22C。

## B. 存储方案确认
- 决策：当前版本继续采用“增强存档 JSON”为单一权威存储。
- 理由：
  - 现有 `save_load` 已具备迁移与兼容路径，回归测试覆盖完善。
  - 本轮目标是 V2 功能闭环与稳定上线，避免引入 sqlite 迁移风险。
- 后续：若出现存档体积、检索性能或并发需求瓶颈，再进入 V3 的 sqlite 迁移专题。

## C. 第一迭代范围确认
- 决策：第一迭代范围按建议执行，并已完成。
- 范围：WP-1 + WP-2 + WP-4.1/4.2。
- 状态：对应任务在 `tasks_v2.md` 已标记完成。

## D. 验收指标确认
- 一致性违例率：通过一致性校验器 V2（含风险分）与回退策略控制。
- 重复率：通过重复段落检测（近窗 + 跨章）与修复文本桥接控制。
- 导出事实覆盖率：通过“真实经历导出”路径与事件日志/章节摘要回写保障。
- 结果：全量门禁通过（前端 42 tests，Rust 286 tests，clippy -D warnings）。

## E. 发布策略确认
- 决策：采用“内部测试包 -> 小流量灰度 -> 正式包”。
- 建议门禁：
  - 灰度前：`npm test` + `cargo test -q` + `cargo clippy -- -D warnings` 全绿。
  - 灰度阶段：重点关注剧情一致性风险分、存档迁移成功率、性能 P95。
  - 正式发布：灰度稳定后打 tag 并归档发布说明。
